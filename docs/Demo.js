/* generated by Svelte v3.24.0 */
import {
	SvelteComponent,
	append,
	attr,
	create_component,
	destroy_component,
	destroy_each,
	detach,
	element,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	set_style,
	space,
	text,
	transition_in,
	transition_out
} from "/the-js-we-do-not-deserve/svelte/internal.js";

import Disclaimer from "./Disclaimer.js";

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[1] = list[i];
	return child_ctx;
}

// (12:0) {#each plugins as plugin}
function create_each_block(ctx) {
	let li;
	let a;
	let t_value = toPkgName(/*plugin*/ ctx[1]) + "";
	let t;
	let a_href_value;

	return {
		c() {
			li = element("li");
			a = element("a");
			t = text(t_value);
			attr(a, "href", a_href_value = "#" + /*plugin*/ ctx[1]);
		},
		m(target, anchor) {
			insert(target, li, anchor);
			append(li, a);
			append(a, t);
		},
		p: noop,
		d(detaching) {
			if (detaching) detach(li);
		}
	};
}

function create_fragment(ctx) {
	let iframe;
	let iframe_src_value;
	let t0;
	let h2;
	let t2;
	let p0;
	let t5;
	let p1;
	let t7;
	let h3;
	let t9;
	let ul;
	let t10;
	let disclaimer;
	let current;
	let each_value = /*plugins*/ ctx[0];
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	disclaimer = new Disclaimer({});

	return {
		c() {
			iframe = element("iframe");
			t0 = space();
			h2 = element("h2");
			h2.textContent = "How does it work?";
			t2 = space();
			p0 = element("p");
			p0.innerHTML = `<a href="https://babeljs.io/" rel="nofollow">Babel</a>.`;
			t5 = space();
			p1 = element("p");
			p1.textContent = "Just like how we use Babel to transpile ESNext code to browser compatible JavaScript, Babel is used here to bring the JavaScript we don’t deserve.";
			t7 = space();
			h3 = element("h3");
			h3.textContent = "Check out the list of Babel plugin";
			t9 = space();
			ul = element("ul");

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			t10 = space();
			create_component(disclaimer.$$.fragment);
			if (iframe.src !== (iframe_src_value = "https://codesandbox.io/embed/javascript-next-uevyq?expanddevtools=1&fontsize=14&hidenavigation=1&theme=light")) attr(iframe, "src", iframe_src_value);
			set_style(iframe, "width", "100%");
			set_style(iframe, "height", "500px");
			set_style(iframe, "border", "4px solid");
			set_style(iframe, "box-shadow", "5px 5px 0 #2aa3ef");
			set_style(iframe, "overflow", "hidden");
			attr(iframe, "title", "JavaScript Next");
			attr(iframe, "sandbox", "allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts");
		},
		m(target, anchor) {
			insert(target, iframe, anchor);
			insert(target, t0, anchor);
			insert(target, h2, anchor);
			insert(target, t2, anchor);
			insert(target, p0, anchor);
			insert(target, t5, anchor);
			insert(target, p1, anchor);
			insert(target, t7, anchor);
			insert(target, h3, anchor);
			insert(target, t9, anchor);
			insert(target, ul, anchor);

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(ul, null);
			}

			insert(target, t10, anchor);
			mount_component(disclaimer, target, anchor);
			current = true;
		},
		p(ctx, [dirty]) {
			if (dirty & /*plugins, toPkgName*/ 1) {
				each_value = /*plugins*/ ctx[0];
				let i;

				for (i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
					} else {
						each_blocks[i] = create_each_block(child_ctx);
						each_blocks[i].c();
						each_blocks[i].m(ul, null);
					}
				}

				for (; i < each_blocks.length; i += 1) {
					each_blocks[i].d(1);
				}

				each_blocks.length = each_value.length;
			}
		},
		i(local) {
			if (current) return;
			transition_in(disclaimer.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(disclaimer.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(iframe);
			if (detaching) detach(t0);
			if (detaching) detach(h2);
			if (detaching) detach(t2);
			if (detaching) detach(p0);
			if (detaching) detach(t5);
			if (detaching) detach(p1);
			if (detaching) detach(t7);
			if (detaching) detach(h3);
			if (detaching) detach(t9);
			if (detaching) detach(ul);
			destroy_each(each_blocks, detaching);
			if (detaching) detach(t10);
			destroy_component(disclaimer, detaching);
		}
	};
}

function toPkgName(str) {
	return "@lihautan/babel-plugin" + str.replace(/[A-Z]/g, char => "-" + char.toLowerCase());
}

function instance($$self) {
	const plugins = Array.from(window.DATA).filter(name => name.startsWith("Transform"));
	return [plugins];
}

class Demo extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Demo;