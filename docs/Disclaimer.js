/* generated by Svelte v3.24.0 */
import {
	SvelteComponent,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	space
} from "/the-js-not-have/svelte/internal.js";

function create_fragment(ctx) {
	let h2;
	let t1;
	let p0;
	let t3;
	let p1;
	let t5;
	let p2;

	return {
		c() {
			h2 = element("h2");
			h2.textContent = "Disclaimer";
			t1 = space();
			p0 = element("p");
			p0.innerHTML = `<strong>⚠️ DO NOT USE THESE PLUGINS IN PRODUCTION ⚠️</strong>`;
			t3 = space();
			p1 = element("p");
			p1.textContent = "These plugins are for educational purpose only.";
			t5 = space();
			p2 = element("p");
			p2.textContent = "Who says you can’t write Babel plugin for fun? 🤷‍♂️";
		},
		m(target, anchor) {
			insert(target, h2, anchor);
			insert(target, t1, anchor);
			insert(target, p0, anchor);
			insert(target, t3, anchor);
			insert(target, p1, anchor);
			insert(target, t5, anchor);
			insert(target, p2, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h2);
			if (detaching) detach(t1);
			if (detaching) detach(p0);
			if (detaching) detach(t3);
			if (detaching) detach(p1);
			if (detaching) detach(t5);
			if (detaching) detach(p2);
		}
	};
}

class Disclaimer extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Disclaimer;