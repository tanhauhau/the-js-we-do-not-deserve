<iframe
  src="https://codesandbox.io/embed/javascript-next-uevyq?expanddevtools=1&fontsize=14&hidenavigation=1&theme=light"
  style="width:100%; height:500px; border:4px solid; box-shadow: 5px 5px 0 #2aa3ef; overflow:hidden;"
  title="JavaScript Next"
  sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
></iframe>

## How does it work?

[Babel](https://babeljs.io/).

Just like how we use Babel to transpile ESNext code to browser compatible JavaScript, Babel is used here to bring the JavaScript we don't deserve.

### Check out the list of Babel plugin

<ul>
{#each plugins as plugin}
  <li><a href={'#' + plugin}>{toPkgName(plugin)}</a></li>  
{/each}
</ul>

<Disclaimer />

<script>
  import Disclaimer from './Disclaimer.js';
  const plugins = Array.from(window.DATA).filter(name => name.startsWith('Transform'));

  function toPkgName(str) {
    return '@lihautan/babel-plugin' + str.replace(/[A-Z]/g, (char) => '-' + char.toLowerCase());
  }
</script>
